# Plausible Analytics — wired live for precifarm.com in BaseLayout.astro.
# If you need to point at a self-hosted Plausible instance, set the script src in BaseLayout.

# Netlify Forms handles the contact form submission server-side at deploy time.
# No env var is required for the form endpoint.
